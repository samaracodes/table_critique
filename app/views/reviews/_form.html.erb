<%= form_for @review do |f| %>


  <%= f.fields_for :restaurant do |ff| %>
    <%= ff.label "Restaurant Name:" %>
    <%= ff.text_field :name %>
  <% end %>
  <p>

    <%= f.label "Review Title:"%>
    <%= f.text_field :title %>
    <p>

    <%= f.label "Content:" %>
    <%= f.text_area :content %>
    <p>

    <%= f.fields_for :category do |category_fields| %>
      <%= category_fields.label "Create a Category:" %>
      <%= category_fields.text_field :name %>
      <p> OR </p>
      <p> Select a Category: <%= category_fields.collection_select :id, Category.all, :id, :name %></p>
    <% end %>
    <p>

    <ul class="rate-area">
      <input type="radio" id="5-star" name="rating" value="5" /><label for="5-star" title="Amazing">5 stars</label>
      <input type="radio" id="4-star" name="rating" value="4" /><label for="4-star" title="Good">4 stars</label>
      <input type="radio" id="3-star" name="rating" value="3" /><label for="3-star" title="Average">3 stars</label>
      <input type="radio" id="2-star" name="rating" value="2" /><label for="2-star" title="Not Good">2 stars</label>
      <input type="radio" id="1-star" name="rating" value="1" /><label for="1-star" title="Bad">1 star</label>
    </ul>

    
    <p>
    <%= f.submit %>
  <% end %>
